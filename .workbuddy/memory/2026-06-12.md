# 2026-06-12 工作日志

## 停云山居后台 - CloudBase 静态托管部署
- **部署命令**: `tcb app deploy tysj8 --force`（在 tysj8/ 目录下执行）
- **环境 ID**: `cloud1-d6gzs6wuu4b4e902e`
- **版本**: tysj8-012
- **访问地址**: `https://tysj8-cloud1-d6gzs6wuu4b4e902e.webapps.tcloudbase.com/`
- **CLI 版本**: 3.5.5
- **说明**: 使用 tcb app deploy 云端构建模式，自动完成依赖安装→构建→上传→绑定路由，无需手动操作本地文件

## 移除所有本地 mock 数据链接
- **删除文件**: `mock/` 目录（6个文件：users.js, home.js, menu.js, rooms.js, activities.js, meal-standards.js）和 `services/mock-reset.js`
- **修改文件**:
  - `services/auth.js` — 移除 `require('../mock/users')`，`guestUser` 内联定义
  - `services/member.js` — 移除 `require('../mock/users')`，移除 `getMockProfile()`，`getMemberProfile` 不再 fallback 到 mock，云不可用时抛错
  - `services/catalog.js` — 移除 `require('../mock/menu')`、`../mock/rooms`、`../mock/meal-standards`，所有 list 方法在云不可用时返回空数组
  - `services/reservation.js` — 移除 `require('../mock/rooms')` 和 `require('../mock/users').memberBenefitAccounts`，`getDiningRoomSelectionLimit` 改为 async 从 catalog 获取，`listAvailableAccommodationRooms` 通过 memberService 获取权益
  - `services/cart.js` — 移除 `require('../mock/menu')`，`getMenuItems` 不再 fallback 到 mockItems
  - `services/home.js` — 移除 `require('../mock/home')`，移除 `getMockHomeContent()`，`getHomeContent` 在云不可用时返回空内容
  - `services/activity-signup.js` — 移除 `require('../mock/activities')`，`listActivitySource` 不再 fallback 到 mock
