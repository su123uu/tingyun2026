# 2026-06-16

## 云函数部署与数据库初始化
- 部署 `initDatabase` 云函数到 CloudBase 环境 `cloud1-d6gzs6wuu4b4e902e`
- 调用 `initDatabase` 成功初始化数据库：27 个集合就绪，种子数据写入（含会员等级、住宿房间、包间、菜品、活动、会员权益等）
- 部署 `adminManage` 云函数
- 注意：bash 环境下 `.\` 前缀会被转义，需省略 `.\` 直接用文件名
