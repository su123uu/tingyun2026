# 2026-06-23 工作日志

## 1. 修复订阅消息通知不生效的 4 个 Bug

### P0: payCallback 云函数 OpenID 字段错误
- `payCallback/index.js` 第 553、679 行读取 `order.created_by_openid`，但 meal_orders 表实际字段是 `notification_openid`
- 修复：改为 `order.notification_openid || order.created_by_openid || order.user_id`

### P0: adminManage 云函数字段列表缺失
- `adminManage/index.js` 的 dining_reservations、accommodation_reservations、meal_orders 字段列表缺少 `created_by_openid`
- 修复：在三处 fields 数组中添加 `created_by_openid`，meal_orders 额外添加 `notification_openid`

### P1: order-confirm.js 死代码 return
- `order-confirm.js` 第 113 行的 `return` 导致支付流程（第 114-164 行）永远不执行，非会员无法在线支付
- 修复：删除死代码，重构 create() 函数统一 pay=true/false 两分支

### P1: order-confirm.js 订阅模板不完整
- 原代码仅调用 `requestMealOrderStatus()`，会员缺少 `memberConsumption` 消费通知
- 修复：根据 effectiveCustomerType 判断，会员调 `requestMealOrderWithConsumption()`，非会员调 `requestMealOrderStatus()`

---

## 2. 用餐预订 + 住宿预订：企业微信通知拆分为两条

**文件**: `tingyun2026/cloudfunctions/notificationManage/index.js`

**改动**:
- `buildStaffMessage` 返回 `{ staff, customer }` 而非单条字符串
- `dining_reservation` → `customer` = `buildDiningConfirmMessage(payload)`
- `accommodation_reservation` → `customer` = `buildRoomConfirmMessage(payload)`
- `sendWeComWebhookNotification` 和 `sendStaffNotification` 各自发两条：第1条订单详情（给店员），第2条客户确认短信（店员直接复制发给客人）
- `staffMessage` 保持向后兼容，只返回 `.staff`

## 2. 住宿预订客户确认模板

**新增函数**: `buildRoomConfirmMessage(payload)`
- 模板：【停云山居】/ 山里人XX您好 / 已为您预定xxx房间 / 日期 / 房间 / 地址 / 电话 / 山居夜宿 枕山而眠
- 日期自动取 `check_in_date` 至 `check_out_date`

**环境变量**: `STORE_ADDRESS` / `STORE_PHONE`（用餐和住宿共用）

**部署**: 重新部署 `notificationManage` 云函数

## 3. 点餐通知菜品换行显示

**文件**: `tingyun2026/cloudfunctions/notificationManage/index.js`

**改动**:
- `mealItemsText` 函数：菜品间分隔符从 `，` 改为 `\n`（换行）
- 单品格式从 `名称x2` 改为 `名称 x 2`（加空格）
- `buildStaffMessage` 中 `菜品：` 后接换行，每个菜独立一行
- 修复了 `buildDiningConfirmMessage` 重复定义的编译错误

## 4. 住宿预订通知优化

**文件**: 
- `tingyun2026/cloudfunctions/notificationManage/index.js` — 日期拆为"入住日期"和"离店日期"两行
- `tingyun2026/cloudfunctions/reservationManage/index.js` — 住宿预订提交时不再发企业微信通知，改为仅付款成功时通知（payCallback 已处理）

**部署**: 重新部署 `notificationManage` 云函数

## 5. 通知链统一：会员与散客同步

**改动**:
- `reservationManage/index.js` 用餐和住宿的 `pending_offline` 分支中，加入 `sendStaffNotification`（会员下单即通知，与 payCallback 中散客付款通知一致）
- 通知链统一为：下单/付款 → 企业微信订单详情 + 打印 → 店员确认 → 客户确认短信
- 去掉 `buildDiningConfirmMessage` 和 `buildRoomConfirmMessage` 中的 `【停云山居】` 抬头
- 住宿确认模板日期拆为"入住：xxx"和"离店：xxx"两行
- 客户确认短信仅在 `event.status === 'confirmed'` 时生成

**部署**: `reservationManage`、`notificationManage`、`payCallback` 三个云函数
