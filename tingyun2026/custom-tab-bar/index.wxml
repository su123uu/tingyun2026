<view class="tab-bar">
  <view wx:for="{{list}}" wx:key="pagePath" class="tab-item {{selected === index ? 'active' : ''}}" data-path="{{item.pagePath}}" bindtap="switchTab">
    <t-icon class="tab-icon" name="{{item.icon}}" size="42rpx" />
    <text>{{item.text}}</text>
  </view>
</view>
